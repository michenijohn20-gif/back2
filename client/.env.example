# Local dev: leave empty to use Vite’s proxy (same origin → localhost:4000).
# Production (e.g. Netlify): set to your deployed API origin, no trailing slash.
# VITE_API_URL=https://your-api.onrender.com

# Optional Google Identity Services integration (pairs with GOOGLE_* server env)
# VITE_GOOGLE_CLIENT_ID=

GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
