# Defaults to relative "/api", which uses the Vite dev proxy pointing at localhost:4000.
# Uncomment and set explicitly if you bypass the proxy.
# VITE_API_URL=http://localhost:4000

# Optional Google Identity Services integration (pairs with GOOGLE_* server env)
# VITE_GOOGLE_CLIENT_ID=
